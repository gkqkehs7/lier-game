[build]
builder = "nixpacks"
buildCommand = "npm install && cd packages/shared && npx tsc && cd ../../apps/server && npx tsc"

[deploy]
startCommand = "node apps/server/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
