[build]
builder = "nixpacks"
buildCommand = "cd ../.. && npm install && cd packages/shared && npx tsc && cd ../../apps/server && npx tsc"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
